package anjali;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.Test;

public class AnjaliDemo {
	@Test 
	public void demo() {
		WebDriver driver=new ChromeDriver();
		driver.get("");
		WebDriver driver=new ChromeDriver();
	}
}
